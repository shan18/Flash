This directory contains the code used to train models.

Rename the file `credentials-sample.py` to `credentials.py` and add your own values.

Put `glove.6B.100d.txt` file inside hte text_classification folder.
